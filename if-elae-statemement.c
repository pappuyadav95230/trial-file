//1.check no is possitive or negative
/*#include<stdio.h>
int main()
{
   /* int b;
    printf("enter number\n");
    scanf("%d",&b);
    if (b>0)
    printf("number is positive");
    return 0;
//2.write a program to check possitive or negative .
  /*  int number;
    printf("enter a value\n");
    scanf("%d",&number);
    if(number % 2==0)
    {
       printf("the no. is even");
    }
    else
    {
        printf("the no. is odd");
    }
    return 0;
/*3. write a program to check large number between two number.
    int one,two;
    printf("enter two number\n");
    scanf("%d%d",&one,&two);
    if (one>two)
    {
        printf("one is large no.\n%d",one);
    }
    else
    {
        printf("two is large no.\n",&two);
    }
    return 0;
    /* nested if-else statement
    
    find the greatest no. brtween three numbers.
    int a,b,c;
    printf("enetr the three value\n");
    scanf("%d%d%d",&a,&b,&c);
    if (a>b)
    {
        if (a>c)
        {
            printf("a is the largest number\n%d",a);

        }
        else 
        {
            printf("c is largest number\n%d",c);
        }
    }
    else
    {
        if (b>c)
        {
            printf("b is the largest no.%d ",b);
        }
        else
        {
            printf("c is the largest no.%d",c);
        }
    }
    return 0;*/
    //4. to check whether a year is leap or not.
   /* int year;
    printf("enter year\n");
    scanf("%d",&year);
    if (year % 4==0)
    printf("%d is a leap year",year);
    else 
    printf("%d is not year",year);
    return 0;
}*/
// write a program to check given number possitive or negative 
